{
  "name": "connex_e2e",
  "version": "0.1.0",
  "scripts": {
    "test": "protractor build\\protractor.config.js",
    "ps:test": "npm run test -- --suite=all_en_reg --Env=staging --Grid=true",
    "ps:retry": "npm run test -- --suite=failed --Env=staging --Grid=true"
  },
  "dependencies": {
    "optimist": "^0.6.1"
  }
}
